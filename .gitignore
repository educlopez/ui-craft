ui-craft-workspace/
CLAUDE.md
.DS_Store
**/.DS_Store
node-compile-cache/
.atl/
node_modules/
mcp/node_modules/

# MCP build output (rebuilt on prepublish)
mcp/dist/

# CLI: per-harness assets are hand-authored & committed under cli/assets/<harness>/.

# CLI binary artifact
cli/ui-craft

# goreleaser local snapshot output
dist/

# Local tooling + out-of-scope artifacts (not part of the repo)
.codegraph/
.cursor/

# A contributor's local Codex MCP registration, not repo content. It was committed by
# accident during a mirror sync and carried a stale pin that failed check-version-mentions.
# The repo's own registration is .mcp.json / .mcp.json.example; .codex/skills/ stays tracked
# because those are the shipped mirrors.
.codex/config.toml
openspec/
video/

# Build-eval run artifacts (per-run, not shared)
results/
