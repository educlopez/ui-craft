ui-craft-workspace/
CLAUDE.md
.DS_Store
**/.DS_Store
node-compile-cache/
.atl/
node_modules/
mcp/node_modules/

# MCP build output (rebuilt on prepublish)
mcp/dist/

# CLI: harness mirrors are committed directly (gentle-ai model — no gen-before-build).
# Run `make gen-mirrors` to regenerate, then commit the result.
# Drift from skills/ is caught at PR time by the check-mirrors CI step.

# CLI binary artifact
cli/ui-craft
